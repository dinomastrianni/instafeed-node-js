'use strict';

// Module Dependencies
const { instafeed } = require('./src/instafeed');
const { refreshToken } = require('./src/refreshToken');

// Module Exports
module.exports = {
    instafeed,
    refreshToken
}